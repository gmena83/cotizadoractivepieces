# API Keys for External Services
OPENAI_API_KEY=
GOOGLE_GEMINI_API_KEY=
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=

# Slack Configuration
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=

# Google Sheets Configuration
GOOGLE_SHEETS_SPREADSHEET_ID=
# Note: Google APIs (Sheets, Gmail) require OAuth 2.0 credentials.
# These will be stored in a credentials.json file.
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/credentials.json

# Gmail Configuration
# (Uses the same Google application credentials as above)

# PDF Service (not an external API, but could have config)
# No variables needed for now.
